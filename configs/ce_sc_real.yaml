exp: real_dev
seed: 42

data:
  raw_dir: data/raw_real
  interim_dir: data/interim_real
  processed_dir: data/processed
  sent_max_len: 256

train:
  lr: 1e-3
  batch_size: 64
  epochs: 100
  patience: 20
  use_amp: true
  amp_dtype: bf16
  grad_accum: 1
  shuffle: true
